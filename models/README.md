# Local Models

Model weights are stored locally and are not committed to Git.

Expected layout model path:

```text
models/layout/pp-doclayoutv3/
```

Docker Compose mounts this directory into model-backed services as `/models`.
